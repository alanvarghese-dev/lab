// healthcheck.js
// A simple health check script that exits successfully.
// In a real application, this would perform actual checks,
// e.g., connect to a database, check an API endpoint, etc.

console.log("Health check successful.");
process.exit(0);