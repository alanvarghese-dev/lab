apiVersion: v1
kind: Service
metadata:
  name: kubernetes-deployment-service
spec:
  selector:
    app: kubernetes-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080

  type: NodePort
